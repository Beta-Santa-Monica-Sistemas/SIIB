@model IEnumerable<Beta_System.Models.C_articulos_catalogo>

@{
    List<int> permisos = Session["PermisosModulo"] as List<int>;
}

@if (Model != null || Model.Count() > 0)
{
    <table class="table table-striped table-bordered w-100" id="tabla_articulos_precio" width="100%">
        <thead>
            <tr class="bg-beta text-center">
                <th>Clave</th>
                <th>Artículo</th>
                <th>Medida</th>
                <th>Tipo</th>
                <th>Clasificación</th>
                <th>Rubro</th>
                <th>Asignar</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr class="text-center">
                    <td id="@item.id_articulo">@item.clave</td>
                    <td>@item.nombre_articulo</td>
                    <td>@item.C_unidades_medidas.unidad_medida</td>
                    <td>@item.C_articulos_tipos.nombre_tipo</td>
                    <td>@item.C_articulos_clasificaciones.nombre_clasificacion</td>
                    <td>@item.C_articulos_tipos_requisicion.tipo_requisicion</td>
                    <td class="text-center">
                        <button class="btn btn_beta_icon" onclick="CargarInfoArticuloSinPrecio(@item.id_articulo);"><i class="fa fa-plus"></i></button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="col-md-12">
        <h3 style="color:red">No se encontro informacion de los articulos</h3>
    </div>
}