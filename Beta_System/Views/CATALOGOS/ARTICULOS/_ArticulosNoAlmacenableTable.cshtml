@model IEnumerable<Beta_System.Models.C_articulos_catalogo>

@{
    List<int> permisos = Session["PermisosModulo"] as List<int>;
}

<table class="table table-striped table-bordered w-100" id="tabla_articulos_pendientes" width="100%">
    <thead>
        <tr class="bg-beta text-center">
            <th>Seleccionar</th>
            <th>Clave</th>
            <th>Artículo</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr class="text-center">
                <td>
                    <input id="@item.id_articulo" class="form-control" type="checkbox" style="height:25px; width:25px;" />
                </td>
                <td>@item.clave</td>
                <td>@item.nombre_articulo</td>
            </tr>
        }
    </tbody>
</table>