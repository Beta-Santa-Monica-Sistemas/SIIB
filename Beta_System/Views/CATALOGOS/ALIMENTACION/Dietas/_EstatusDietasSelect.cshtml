@model List<Beta_System.Models.C_alimentacion_dietas_status>

@if (Model.Count() > 0)
{
    foreach (var item in Model)
    {
        <option value="@item.id_dieta_status">@item.nombre_status</option>
    }
}

