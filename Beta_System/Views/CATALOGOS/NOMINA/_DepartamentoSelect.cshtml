@model IEnumerable<Beta_System.Models.C_nomina_departamentos>

@if (Model.Count() > 0)
{
    foreach (var item in Model)
    {
        <option value="@item.id_departamento_empleado">@item.nombre_departamento</option>
    }
}