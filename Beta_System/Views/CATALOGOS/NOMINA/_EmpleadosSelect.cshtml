@model List<Beta_System.Models.C_nomina_empleados>

@if (Model.Count() > 0)
{
    foreach (var item in Model.OrderBy(x => x.Nombres))
    {
        <option value="@item.Empleado_id">@item.Nombres @item.Apellido_paterno @item.Apellido_materno</option>
    }
}